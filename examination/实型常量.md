
# 五大类型

第一，整型常量

第二，实型常量
十进制小数形式：`1.2`
指数形式：`-3.5e-2/-0.35E-1`。错误，`e10`(应该是`1e10`)，`2e2.5`（指数必须是整数）

第三，字符常量
普通字符：`a`
转义字符：`\n`,`\101`（八进制101，即65，`A`），`\x41`（十六进制，即65，`A`）

第四，字符串常量
`"A"`

第五，符号常量
`#define PI 3.1416`的`PI`(注意没有分号)。

